
jbackup:
  global:
    compress: zip
    verbose: false
    crypt: true
    password: 12345
    splitSize:
  dir:
    test1:
      #path: D:/temp/amdfx
      path: D:/temp/apache-tomcat-9.0.30
      dest: D:/temp/backup_test
      include:
      #include: '**/*.txt,**/*.jsp'
      exclude:
      #exclude: '**/*.html'
      disabled: false

